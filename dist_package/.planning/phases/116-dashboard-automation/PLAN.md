# Phase 116 Plan: Dashboard Automation (aa-Dashboard)

## 🎯 執行目標
實作一鍵自動化指令 `aa-dashboard`，整合偵測、背景服務預熱與瀏覽器跳轉。

---

## 🌊 Wave 1: 核心邏輯實作 (Dashboard Core)
- [ ] **Task 1.1**: 實作 `scripts/aa_dashboard.py` (基礎架構)。
- [ ] **Task 1.2**: 建立 `is_port_active(9999)` 偵測函式 (Socket based)。
- [ ] **Task 1.3**: 建立伺服器啟動邏輯 `subprocess.Popen(["python", "-m", "http.server", "9999"])`。
  - *關鍵*: 使用 `CREATE_NEW_PROCESS_GROUP` (Windows) 以便在主指令結束後伺服器依然運行。
- [ ] **Task 1.4**: 使用 `webbrowser` 模組導向目標 URL。

## 🌊 Wave 2: 優化與驗證 (UAT)
- [ ] **Task 2.1**: 測試背景伺服器分離 (確保關閉 Terminal 視窗後伺服器不失效)。
- [ ] **Task 2.2**: UAT 1 - 冷啟動 (Cold Boot) 驗證。
- [ ] **Task 2.3**: UAT 2 - 熱啟動 (Hot Boot) 驗證。

---

## ✅ 驗證標準 (UAT)
1. **[UAT-1] 冷啟動測試**:
   - 前置: 確認 `localhost:9999` 無 HTTP 回應。
   - 動作: 執行 `python scripts/aa_dashboard.py`。
   - 結果: 自動啟動背景伺服器，並開啟瀏覽器進入 Dashboard。
2. **[UAT-2] 熱啟動測試**:
   - 前置: 確認 `localhost:9999` 已在運行中。
   - 動作: 執行 `python scripts/aa_dashboard.py`。
   - 結果: 不會產生新的伺服器進程，直接開啟瀏覽器分頁。

---
*Plan generated by Antigravity v1.8.0 - Phase 116 Plan*
